h1 = t('.delete_screenshot', default: 'Delete screenshot "%{name}"', name: @screenshot.name)
p = button_to [ :admin, @project, @screenshot ], method: :delete, class: 'btn btn-danger' do
    = t('.delete_screenshot')
