h1 = t('.delete_time_slot', default: 'Delete time slot "%{started_at}" — "%{ended_at}"', started_at: @time_slot.started_at, ended_at: @time_slot.ended_at)

p = button_to [ :admin, @project, @time_slot ], method: :delete, class: 'btn btn-danger' do
  = t('.destroy_time_slot')
